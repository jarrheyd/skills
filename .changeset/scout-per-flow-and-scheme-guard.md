---
"jarrheyd-skills": patch
---

scout-run runs flows one invocation each by default (Maestro aborts a multi-flow run on an unresolved addMedia path with nothing executed) with a portable perl-alarm timeout, and guard-env accepts app-scheme deep links (kapwa://...) since they open the already-approved installed build.
